name: Disabled Render deploy helper

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Render deploy workflow disabled. Re-enable or recreate a safe workflow when needed."
